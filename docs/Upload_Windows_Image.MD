# Windows Image Preparation Steps

Windows images are supported through this plugin and cloud init needs to be configured through cloudbase for Morpheus to setup the Virtual machine during first boot.
