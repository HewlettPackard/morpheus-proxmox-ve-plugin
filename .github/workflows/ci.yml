# Copyright 2025 Hewlett Packard Enterprise Development LP
#
name: CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

# Cancel any running jobs for this same ref/PR
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        cache-read-only: false

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Run CodeNarc code quality check
      run: ./gradlew codenarcMain --no-daemon --stacktrace
      continue-on-error: true

    - name: Upload CodeNarc reports
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: codenarc-reports
        path: build/reports/codenarc/
        retention-days: 7
        if-no-files-found: ignore

    - name: Run build
      run: ./gradlew build --no-daemon --stacktrace

    - name: Install Trivy
      run: |
        wget https://github.com/aquasecurity/trivy/releases/download/v0.33.1/trivy_0.33.1_Linux-64bit.deb
        sudo dpkg -i trivy_0.33.1_Linux-64bit.deb

    - name: Run Trivy vulnerability scan on shadow JAR
      run: |
        SHADOW_JAR=$(find build/libs -name '*-all.jar' -type f | head -1)
        echo "Scanning JAR: $SHADOW_JAR"
        trivy fs --format table --exit-code 1 --severity CRITICAL,HIGH --ignore-unfixed "$SHADOW_JAR"
